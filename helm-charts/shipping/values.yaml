image:
  repository: robotshopacr.azurecr.io/shipping
  tag: latest  # Will be overridden by umbrella chart values
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 30m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 1Gi

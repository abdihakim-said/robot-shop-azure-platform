image:
  repository: "{{ .Values.global.imageRegistry }}/shipping"
  tag: "{{ .Values.global.imageTag }}"
  pullPolicy: "{{ .Values.global.imagePullPolicy }}"

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 30m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 1Gi

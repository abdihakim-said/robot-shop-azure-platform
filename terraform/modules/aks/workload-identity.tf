# Workload Identity resources removed - using VM managed identity instead
# VM managed identity (kubelet identity) is more reliable with CSI driver
# and already has Key Vault access configured in the keyvault module
